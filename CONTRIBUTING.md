# دليل المساهمة (Contributing Guidelines)

نحن نرحب بمساهمات جميع الطلاب! إذا كان لديك أداة مفيدة، سكربت، أو مشروع ترغب في إضافته إلى **StudentDev-Toolkit**، يرجى اتباع الخطوات التالية:

## كيف تساهم؟

1. **قم بعمل Fork للمستودع**: اضغط على زر `Fork` في أعلى يمين الصفحة لنسخ المشروع إلى حسابك.
2. **استنسخ المستودع (Clone)**: قم بتحميل نسختك إلى جهازك المحلي.
   ```bash
   git clone https://github.com/YOUR-USERNAME/univer-StudentDev.git
   ```
3. **أنشئ فرعاً جديداً (Branch)**: يفضل دائماً العمل على فرع جديد لكل إضافة.
   ```bash
   git checkout -b feature/your-feature-name
   ```
4. **أضف ملفاتك**: ضع ملفاتك في المجلد المناسب (مثلاً `programming/web/html` أو `tools`).
5. **قم بعمل Commit**: اكتب رسالة واضحة تشرح ما قمت بإضافته.
   ```bash
   git commit -m "إضافة أداة جديدة لـ [اسم الأداة]"
   ```
6. **ارفع التعديلات (Push)**:
   ```bash
   git push origin feature/your-feature-name
   ```
7. **افتح Pull Request**: اذهب إلى المستودع الأصلي وافتح طلب سحب (Pull Request) وسنقوم بمراجعته ودمجه!

## شروط قبول المساهمات

- تأكد من وضع الكود في المجلد الصحيح.
- لا تقم برفع ملفات النظام أو المجلدات الثقيلة مثل `node_modules` (تأكد من استخدام `.gitignore`).
- أضف تعليقات توضيحية داخل الكود ليسهل على الطلاب الآخرين فهمه.

شكراً لمساهمتك في إثراء هذا المحتوى للطلاب! 🚀
